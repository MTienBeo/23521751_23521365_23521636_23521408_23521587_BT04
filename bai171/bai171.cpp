#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int giatri_max_trongtunghang(int a[][100], int hang, int cot)
{
    int i, j;
    int max = a[0][0];
    for (i = 0; i < hang; i++)
        for (j = 0; j < cot; j++)
            if (a[0][j] > m)
            {
                max = a[0][j];
            }
    return max;
}